{
  "name": "playwright-captcha-aware",
  "version": "1.0.0",
  "type": "module",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.js",
    "install-playwright-browsers": "playwright install --with-deps"
  },
  "dependencies": {
    "express": "^4.19.2",
    "playwright": "^1.40.0"
  }
}
