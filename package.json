{
  "name": "cf-bypass",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js" 
"build": "npx playwright install --with-deps"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.47.0"
  }
}
